---
- name: Install Docker on all hosts
  hosts: all
  become: true
  vars_files:
    - vars.yml

  tasks:
    - name: Update yum packages
      yum:
        name: "*"
        state: latest

    - name: Install Docker
      yum:
        name: docker
        state: present

    - name: Start and enable Docker service
      systemd:
        name: docker
        state: started
        enabled: true

    - name: Add ec2-user to docker group
      user:
        name: ec2-user
        groups: docker
        append: true

    - name: Install Docker Compose plugin
      shell: |
        mkdir -p /usr/local/lib/docker/cli-plugins
        curl -SL https://github.com/docker/compose/releases/download/v2.24.0/docker-compose-linux-x86_64 \
          -o /usr/local/lib/docker/cli-plugins/docker-compose
        chmod +x /usr/local/lib/docker/cli-plugins/docker-compose
      args:
        creates: /usr/local/lib/docker/cli-plugins/docker-compose

# ─── Deploy catalog-service ───────────────────────────────────
- name: Deploy catalog-service
  hosts: catalog
  become: true
  vars_files:
    - vars.yml

  tasks:
    - name: Pull catalog-service image
      community.docker.docker_image:
        name: "{{ catalog_image }}"
        source: pull

    - name: Run catalog-service container
      community.docker.docker_container:
        name: catalog-service
        image: "{{ catalog_image }}"
        state: started
        restart_policy: always
        ports:
          - "8082:8082"
        env:
          DB_HOST: "{{ db_host }}"
          DB_PORT: "{{ db_port }}"
          DB_NAME: "catalogdb"
          DB_USER: "{{ db_user }}"
          DB_PASSWORD: "{{ db_password }}"
          AWS_REGION: "{{ aws_region }}"

# ─── Deploy order-service ─────────────────────────────────────
- name: Deploy order-service
  hosts: order
  become: true
  vars_files:
    - vars.yml

  tasks:
    - name: Pull order-service image
      community.docker.docker_image:
        name: "{{ order_image }}"
        source: pull

    - name: Run order-service container
      community.docker.docker_container:
        name: order-service
        image: "{{ order_image }}"
        state: started
        restart_policy: always
        ports:
          - "8083:8083"
        env:
          DB_HOST: "{{ db_host }}"
          DB_PORT: "{{ db_port }}"
          DB_NAME: "orderdb"
          DB_USER: "{{ db_user }}"
          DB_PASSWORD: "{{ db_password }}"
          AWS_REGION: "{{ aws_region }}"
          SQS_QUEUE_URL: "{{ sqs_queue_url }}"
          CATALOG_SERVICE_URL: "http://{{ catalog_service_ip }}:8082"

# ─── Deploy notification-service ──────────────────────────────
- name: Deploy notification-service
  hosts: notification
  become: true
  vars_files:
    - vars.yml

  tasks:
    - name: Pull notification-service image
      community.docker.docker_image:
        name: "{{ notification_image }}"
        source: pull

    - name: Run notification-service container
      community.docker.docker_container:
        name: notification-service
        image: "{{ notification_image }}"
        state: started
        restart_policy: always
        ports:
          - "8084:8084"
        env:
          AWS_REGION: "{{ aws_region }}"
          SQS_QUEUE_URL: "{{ sqs_queue_url }}"
